/**
 * Product tables start in Phase 1 (business/staff/services) and Phase 2 (appointments).
 * Phase 0 keeps an empty schema so Drizzle and migrate tooling can exist without inventing domain.
 */
export {}
